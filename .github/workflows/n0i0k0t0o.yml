name: n0i0k0t0o

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
#---------------------------------------------------update 
    - name: update
      run: sudo apt update
#---------------------------------------------------n0i0k0t0o
    - name: n0i0k0t0o install 
      run: Fsudo apt install nikto
    - name: n0i0k0t0o scan
      run: |
       nikto -h alexchanger.com
