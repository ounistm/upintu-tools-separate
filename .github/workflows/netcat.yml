name: netcat

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
#---------------------------------------------------update 
    - name: update
      run: sudo apt update
#---------------------------------------------------netcat
    - name: netcat install 
      run:  sudo apt install netcat
    - name: netcat scan
      run: |
       netcat
